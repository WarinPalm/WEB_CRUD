type NavbarMainProps = {
  setCollapsed: (collapsed: boolean) => void;
  collapsed: boolean;
};
const NavbarMain = ({ setCollapsed, collapsed }: NavbarMainProps) => {
  return (
    <>
    </>
  );
};

export default NavbarMain;
