import "./sidebar.css";
type SidebarProps = {
  collapsed: boolean;
  setCollapsed: any;
};

const SidebarMain = ({ collapsed }: SidebarProps) => {
 
  return (
    <>
    </>
  );
};

export default SidebarMain;
